% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_utils.R
\name{atlas_manipulation}
\alias{atlas_manipulation}
\alias{atlas_region_remove}
\alias{atlas_region_contextual}
\alias{atlas_region_op}
\alias{atlas_context_remove}
\alias{atlas_region_rename}
\alias{atlas_region_keep}
\alias{atlas_core_add}
\alias{atlas_view_remove}
\alias{atlas_view_keep}
\alias{atlas_view_remove_region}
\alias{atlas_view_remove_small}
\alias{atlas_view_select}
\alias{atlas_view_gather}
\alias{atlas_view_reorder}
\title{Manipulate brain atlas regions and views}
\usage{
atlas_region_remove(atlas, pattern, match_on = c("region", "label"))

atlas_region_contextual(
  atlas,
  pattern,
  match_on = c("region", "label"),
  ignore.case = TRUE
)

atlas_region_op(
  atlas,
  x,
  y,
  action = c("difference", "intersection", "union", "symdifference"),
  into = NULL,
  match_on = c("label", "region"),
  colour = NULL
)

atlas_context_remove(atlas)

atlas_region_rename(
  atlas,
  pattern,
  replacement,
  match_on = c("region", "label")
)

atlas_region_keep(atlas, pattern, match_on = c("region", "label"))

atlas_core_add(atlas, data, by = "region")

atlas_view_remove(atlas, views)

atlas_view_keep(atlas, views)

atlas_view_remove_region(
  atlas,
  pattern,
  match_on = c("label", "region"),
  views = NULL
)

atlas_view_remove_small(
  atlas,
  min_area,
  views = NULL,
  scope = c("region", "piece"),
  labels = NULL,
  exclude = NULL
)

atlas_view_select(atlas, threshold = 0.5, weights = NULL)

atlas_view_gather(atlas, gap = 0.15)

atlas_view_reorder(atlas, order, gap = 0.15)
}
\arguments{
\item{atlas}{A \code{ggseg_atlas} object}

\item{pattern}{Character pattern to match. Uses
\code{grepl(..., ignore.case = TRUE)}.}

\item{match_on}{Column to match against: \code{"region"} or \code{"label"}.}

\item{ignore.case}{For \code{atlas_region_contextual()}: passed to \code{\link[=grepl]{grepl()}}.
Defaults to \code{TRUE} for backwards compatibility, but note that a context
pattern like \code{"Thalamus"} then also matches focus labels such as
\code{"hypothalamus"}; set \code{FALSE} (and/or anchor the pattern) when that
matters.}

\item{x, y}{For \code{atlas_region_op()}: patterns selecting the two operands.}

\item{action}{For \code{atlas_region_op()}: the boolean operation to apply.}

\item{into}{For \code{atlas_region_op()}: label for the result region.}

\item{colour}{For \code{atlas_region_op()}: optional fill for \code{into}. When
supplied, \code{into} is registered in core and palette; when \code{NULL}, the
result is contextual geometry only.}

\item{replacement}{For \code{atlas_region_rename()}: replacement string or
function.}

\item{data}{For \code{atlas_core_add()}: data.frame with metadata to join.}

\item{by}{For \code{atlas_core_add()}: column(s) to join by. Default \code{"region"}.}

\item{views}{For view functions: character vector of view names or
patterns. Multiple values collapsed with \code{"|"} for matching.}

\item{min_area}{For \code{atlas_view_remove_small()}: minimum area to keep.
What it applies to depends on \code{scope}; with the default
\code{scope = "region"} context geometries are never removed.}

\item{scope}{Whether \code{min_area} applies to a label's whole geometry in a
view (\code{"region"}, the default) or to each disconnected piece
(\code{"piece"}).}

\item{labels, exclude}{For \code{atlas_view_remove_small()}: optional regex
scoping which labels are considered. \code{labels} restricts removal to
matching labels, \code{exclude} spares them. Only one may be given. Useful
with \code{scope = "piece"}, where a thin structure such as a cortical ribbon
has legitimately small pieces that should not be treated as specks.}

\item{threshold}{For \code{atlas_view_select()}: minimum share, between 0 and 1,
of a region's best-view area that a view must hold to keep drawing it.
Higher values are more aggressive.}

\item{weights}{For \code{atlas_view_select()}: optional named numeric vector of
per-view multipliers applied before comparing areas. Overrides the
automatic hemisphere-coverage weighting for the views named.}

\item{gap}{Proportional gap between views (default 0.15 = 15\% of max width).}

\item{order}{For \code{atlas_view_reorder()}: character vector of desired
view order. Unspecified views appended at end.}
}
\value{
Modified \code{ggseg_atlas} object
}
\description{
Functions for modifying brain atlas objects. These cover three areas:
}
\details{
\strong{Region manipulation} modifies which regions are active in the atlas:
\itemize{
\item \code{atlas_region_remove()}: completely remove regions
\item \code{atlas_region_contextual()}: keep geometry but remove from core/palette
\item \code{atlas_context_remove()}: drop all contextual sf geometry
\item \code{atlas_region_rename()}: rename regions in core
\item \code{atlas_region_keep()}: keep only matching regions
\item \code{atlas_region_op()}: combine two regions' geometry with a boolean op
(difference / intersection / union / symdifference)
}

\strong{View manipulation} modifies the 2D sf geometry data:
\itemize{
\item \code{atlas_view_remove()}: remove entire views
\item \code{atlas_view_keep()}: keep only matching views
\item \code{atlas_view_remove_region()}: remove specific region geometry from sf
\item \code{atlas_view_remove_small()}: remove small regions, or stray specks
\item \code{atlas_view_select()}: keep each region only in the views that show it
well
\item \code{atlas_view_gather()}: reposition views to close gaps
\item \code{atlas_view_reorder()}: change view order
}

\strong{Core manipulation} modifies atlas metadata:
\itemize{
\item \code{atlas_core_add()}: join additional metadata columns
}
}
\section{Functions}{
\itemize{
\item \code{atlas_region_contextual()}: Keep geometry for visual context but remove
from core, palette, and 3D data. Context geometries render grey and don't
appear in legends. Contextual rows are moved behind the remaining core
regions so focus regions draw on top where they overlap in projection.
Operates on whichever 2D representation the atlas carries (\code{sf} and/or
\code{polygons}), keeping both in sync, and needs no \code{sf} for a polygon atlas.

\item \code{atlas_region_op()}: Combine two sets of region geometry with a
vector boolean operation (per view), writing the result to a new region
\code{into}. \code{x} and \code{y} are patterns matched against \code{match_on}; within each
view their matching geometries are unioned, then combined via \code{action}:
\code{"difference"} (x minus y, e.g. punching white matter out of a cortex
silhouette), \code{"intersection"}, \code{"union"}, or \code{"symdifference"}. Inputs are
left in place; any existing \code{into} geometry is replaced. With a \code{colour},
\code{into} becomes a legended core region; otherwise it stays contextual
(grey) and is drawn behind the core regions. Boolean ops need a geometry
engine, so this is the one manipulation helper that always requires \code{sf}
installed; a polygon-only atlas is rehydrated for the operation and the
result is returned in polygon form.

\item \code{atlas_context_remove()}: Drop all contextual sf geometry — every
sf row whose \code{label} is not present in \code{core}. Covers labels marked
via \code{\link[=atlas_region_contextual]{atlas_region_contextual()}} plus pipeline-generated outlines
(\code{cortex_}, \code{Background}, \code{unknown}). Remaining views are re-packed
via \code{\link[=atlas_view_gather]{atlas_view_gather()}} so the plot focuses tightly on the
labelled regions.

\item \code{atlas_region_rename()}: Rename regions matching a pattern. Only
ever writes to the \code{region} column, never to \code{label}. \code{match_on} chooses
which column the pattern is matched and substituted against: the default
\code{"region"} rewrites the display names in place, while \code{"label"} derives
them from the source identifiers, so
\code{atlas_region_rename(atlas, "^ctx-lh-", "", match_on = "label")} turns
label \code{ctx-lh-superiorfrontal} into region \code{superiorfrontal}. If
\code{replacement} is a function, it receives the matched values of that
column and returns the new region names.

\item \code{atlas_region_keep()}: Keep only matching regions. Non-matching
regions are removed from core, palette, and 3D data but sf geometry
is preserved for surface continuity.

\item \code{atlas_core_add()}: Join additional metadata columns to
atlas core.

\item \code{atlas_view_remove()}: Remove views matching pattern from sf
data. Remaining views are re-packed via \code{\link[=atlas_view_gather]{atlas_view_gather()}} so
the layout stays tight.

\item \code{atlas_view_keep()}: Keep only views matching pattern.

\item \code{atlas_view_remove_region()}: Remove specific region geometry from sf
data only. Core, palette, and 3D data are unchanged. Views are
re-packed via \code{\link[=atlas_view_gather]{atlas_view_gather()}} in case any view shrank.

\item \code{atlas_view_remove_small()}: Remove geometry below a minimum area
threshold. With \code{scope = "region"} (the default) a label's whole geometry
in a view is removed when its combined area is too small, and context
geometries (labels not in core) are never removed. With
\code{scope = "piece"} individual disconnected pieces are removed while the
rest of the region stays -- use this to clear stray specks left by
volumetric projection. A region's largest piece in a view is always kept,
so no region disappears, and context is cleaned too. Optionally scope to
specific views. Views are re-packed via \code{\link[=atlas_view_gather]{atlas_view_gather()}} in case any
view shrank.

\item \code{atlas_view_select()}: Keep each region only in the views that show
it well.

A slice or projection slab catches a structure in cross-section as readily
as along its length, so most regions leave a sliver in most views. That
leaves every panel cluttered and every region drawn several times over. A
region is kept only where it is substantially represented: in any view
holding at least \code{threshold} of the area it reaches in its best view.

Regions are compared as a whole, across all the labels that share a
\code{region} in \code{core}, so bilateral structures stay together -- assigning
left and right independently splits pairs across panels, which reads as
an error rather than a choice. Every region is guaranteed to survive in
at least one view, and context geometry (labels absent from \code{core}, such
as a cortical outline) is never touched.

Single-hemisphere views are the awkward case: a sagittal panel cuts one
hemisphere while axial and coronal panels show both, so on raw area it
loses every comparison and empties out. Such views are detected from the
hemispheres actually present in each view and their areas scaled to
match, so they compete fairly. Pass \code{weights} to override.

\item \code{atlas_view_gather()}: Reposition remaining views to close gaps
after view removal.

\item \code{atlas_view_reorder()}: Reorder views and reposition. Views not
in \code{order} are appended at end.

}}
\examples{
dk() |>
  atlas_region_remove("bankssts") |>
  atlas_region_keep("frontal", match_on = "region")

}
\seealso{
Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}

Other atlas manipulations: 
\code{\link{atlas_structure_reorder}()}
}
\concept{atlas manipulations}
